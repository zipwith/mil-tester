struct S / 16
  [ x :: Stored Word  {- offset=0 -}
  | y :: Stored Word  {- offset=4 -}
  | z :: Stored Word  {- offset=8 -}
  | t :: Stored Word  {- offset=12 -} ]
  aligned 4

data Unit
  = Unit

-----------------------------------------
-- not recursive
entrypoint s :: Word
s <- area S aligned 4

-----------------------------------------
-- not recursive
b16 :: [] >>= [Unit]
b16[] =
  t0 <- store32((s, 17))
  t1 <- add((s, 4))
  t2 <- store32((t1, 17))
  t3 <- add((s, 8))
  t4 <- store32((t3, 17))
  t5 <- add((s, 12))
  store32((t5, 17))

-----------------------------------------
-- not recursive
initialize :: [] >>= [Unit]
initialize[] =
  b16[]

-----------------------------------------
-- Entrypoints: s
